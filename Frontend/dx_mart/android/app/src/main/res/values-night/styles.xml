<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app has no dark theme — `AppTheme.light` is the only theme handed
         to `MaterialApp`, so every screen after the splash looks identical
         regardless of the device's system setting. This mirrors
         values/styles.xml rather than a dark background, so a phone in dark
         mode doesn't flash black-then-mint on the way in. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_splash_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>

# OS files
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*.tmp
*.temp

# Logs and runtime output
*.log
error_log

# Environment and secrets
.env
.env.*
# ...but the template documents variable NAMES only and must stay tracked.
!.env.example
!**/.env.example

# Flutter --dart-define-from-file config. The publishable key in here is safe to ship in
# a client build, but URLs/keys differ per environment, so only the template is tracked.
Frontend/*/env/*.json
!Frontend/*/env/*.json.example
*.pem
*.key
*.p12
*.p8

# PHP / backend dependencies and caches
vendor/
composer.lock
phpunit.xml

# Flutter / Dart
**/.dart_tool/
**/.packages
**/.pub-cache/
**/build/
**/android/.gradle/
**/android/app/build/
**/ios/Pods/
**/ios/.symlinks/
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/ephemeral/
**/.flutter-plugins
**/.flutter-plugins-dependencies

# Local databases / uploads
*.sqlite
*.db
*.sql~

# Backup / temp files
*.bak
*.orig

# Generated assets or local runtime folders
Backend/**/uploads/
Backend/**/error_log
